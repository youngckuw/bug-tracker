pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            agent {
                docker {
                    image 'snakee/golang-junit:1.21'
                    reuseNode true
                }
            }
            steps {
                dir('bugtracker-backend') {
                    withEnv(["GOCACHE=${env.WORKSPACE}/.cache"]) {
                    sh 'go test -v ./...'
                    }
                }
            }
        }
    }
}
